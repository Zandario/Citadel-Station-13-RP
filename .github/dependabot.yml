# This file is auto-generated by tools/update-dependabot.ps1
version: 2
updates:
- package-ecosystem: "npm"
  directory: "/tools/build"
  schedule:
    interval: "daily"
  target-branch: "dependencies"
  allow:
  - dependency-type: direct
  - dependency-type: production # check only dependencies, which are going to the compiled app, not supporting tools like @vue-cli
- package-ecosystem: "npm"
  directory: "/tools/build/juke"
  schedule:
    interval: "daily"
  target-branch: "dependencies"
  allow:
  - dependency-type: direct
  - dependency-type: production # check only dependencies, which are going to the compiled app, not supporting tools like @vue-cli
- package-ecosystem: "npm"
  directory: "/tools/autowiki"
  schedule:
    interval: "daily"
  target-branch: "dependencies"
  allow:
  - dependency-type: direct
  - dependency-type: production # check only dependencies, which are going to the compiled app, not supporting tools like @vue-cli
- package-ecosystem: "npm"
  directory: "/tgui"
  schedule:
    interval: "daily"
  target-branch: "dependencies"
  allow:
  - dependency-type: direct
  - dependency-type: production # check only dependencies, which are going to the compiled app, not supporting tools like @vue-cli
- package-ecosystem: "npm"
  directory: "/tgui/packages/tgui-panel"
  schedule:
    interval: "daily"
  target-branch: "dependencies"
  allow:
  - dependency-type: direct
  - dependency-type: production # check only dependencies, which are going to the compiled app, not supporting tools like @vue-cli
- package-ecosystem: "npm"
  directory: "/tgui/packages/tgui-dev-server"
  schedule:
    interval: "daily"
  target-branch: "dependencies"
  allow:
  - dependency-type: direct
  - dependency-type: production # check only dependencies, which are going to the compiled app, not supporting tools like @vue-cli
- package-ecosystem: "npm"
  directory: "/tgui/packages/tgui-polyfill"
  schedule:
    interval: "daily"
  target-branch: "dependencies"
  allow:
  - dependency-type: direct
  - dependency-type: production # check only dependencies, which are going to the compiled app, not supporting tools like @vue-cli
- package-ecosystem: "npm"
  directory: "/tgui/packages/tgfont"
  schedule:
    interval: "daily"
  target-branch: "dependencies"
  allow:
  - dependency-type: direct
  - dependency-type: production # check only dependencies, which are going to the compiled app, not supporting tools like @vue-cli
- package-ecosystem: "npm"
  directory: "/tgui/packages/tgui-bench"
  schedule:
    interval: "daily"
  target-branch: "dependencies"
  allow:
  - dependency-type: direct
  - dependency-type: production # check only dependencies, which are going to the compiled app, not supporting tools like @vue-cli
- package-ecosystem: "npm"
  directory: "/tgui/packages/common"
  schedule:
    interval: "daily"
  target-branch: "dependencies"
  allow:
  - dependency-type: direct
  - dependency-type: production # check only dependencies, which are going to the compiled app, not supporting tools like @vue-cli
- package-ecosystem: "npm"
  directory: "/tgui/packages/tgui"
  schedule:
    interval: "daily"
  target-branch: "dependencies"
  allow:
  - dependency-type: direct
  - dependency-type: production # check only dependencies, which are going to the compiled app, not supporting tools like @vue-cli
